services:
  - type: web
    name: rotalucro-api
    runtime: node
    buildCommand: npm install && cd frontend && npm install && npm run build
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
